const express = require("express");

const app = express();

/**
 * Get all users
 */
function getUsers() {
    return null;
}

/**
 * api to return all users
 * @param {Object} req 
 * @param {Object} res 
 */
function index(req,res) {
    
}
app.get("/", index);

